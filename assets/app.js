(function () {
  "use strict";

  var ADMIN_EMAIL = "director@wise-jmco.com";
  var INQUIRY_GAS_ENDPOINT = "PASTE_GAS_WEBAPP_URL_HERE";
  var TXT = {
    preview: "\u30d7\u30ec\u30d3\u30e5\u30fc\u3067\u898b\u308b", videoTitle: "\u7206\u901f\u30ea\u30b9\u30ad\u30ea\u30f3\u30b0\u52d5\u753b", sampleNote: "Firebase\u8a2d\u5b9a\u5f8c\u306b\u4f1a\u54e1\u9650\u5b9a\u30c7\u30fc\u30bf\u3078\u5207\u308a\u66ff\u308f\u308a\u307e\u3059\u3002", uncategorized: "\u672a\u5206\u985e", youtube: "YouTube", openVideo: "YouTube\u3067\u898b\u308b", noPublished: "\u516c\u958b\u4e2d\u306e\u52d5\u753b\u306f\u307e\u3060\u3042\u308a\u307e\u305b\u3093\u3002", noMatch: "\u6761\u4ef6\u306b\u5408\u3046\u52d5\u753b\u304c\u3042\u308a\u307e\u305b\u3093\u3002", mail: "\u30e1\u30fc\u30eb", name: "\u6c0f\u540d", role: "\u6a29\u9650", status: "\u72b6\u614b", noUsers: "\u4f1a\u54e1\u30c7\u30fc\u30bf\u306f\u307e\u3060\u3042\u308a\u307e\u305b\u3093\u3002", noStaff: "\u30b9\u30bf\u30c3\u30d5\u30c7\u30fc\u30bf\u306f\u307e\u3060\u3042\u308a\u307e\u305b\u3093\u3002", adminOrEditorOnly: "\u7ba1\u7406\u753b\u9762\u306fadmin\u307e\u305f\u306feditor\u6a29\u9650\u304c\u5fc5\u8981\u3067\u3059\u3002", title: "\u30bf\u30a4\u30c8\u30eb", category: "\u30ab\u30c6\u30b4\u30ea", type: "\u7a2e\u5225", publishDate: "\u516c\u958b\u65e5", public: "\u516c\u958b", private: "\u975e\u516c\u958b", noVideos: "\u52d5\u753b\u30c7\u30fc\u30bf\u306f\u307e\u3060\u3042\u308a\u307e\u305b\u3093\u3002", firebaseSave: "Firebase\u8a2d\u5b9a\u5f8c\u306b\u4f1a\u54e1\u4fdd\u5b58\u3092\u5229\u7528\u3067\u304d\u307e\u3059\u3002", enterEmail: "\u30e1\u30fc\u30eb\u30a2\u30c9\u30ec\u30b9\u3092\u5165\u529b\u3057\u3066\u304f\u3060\u3055\u3044\u3002", savedMember: "\u4f1a\u54e1\u3092\u4fdd\u5b58\u3057\u307e\u3057\u305f\u3002", adminOnly: "\u7ba1\u7406\u753b\u9762\u306fadmin\u6a29\u9650\u304c\u5fc5\u8981\u3067\u3059\u3002", previewToast: "Firebase\u672a\u8a2d\u5b9a\u306e\u305f\u3081\u30d7\u30ec\u30d3\u30e5\u30fc\u8868\u793a\u3067\u3059\u3002", previewMsg: "Firebase\u8a2d\u5b9a\u524d\u306e\u305f\u3081\u3001\u516c\u958b\u30d7\u30ec\u30d3\u30e5\u30fc\u3068\u3057\u3066\u8868\u793a\u3067\u304d\u307e\u3059\u3002", noMember: "\u3053\u306e\u30e1\u30fc\u30eb\u30a2\u30c9\u30ec\u30b9\u306f\u4f1a\u54e1\u767b\u9332\u3055\u308c\u3066\u3044\u307e\u305b\u3093\u3002", basic: "\u57fa\u790e\u8b1b\u7fa9", ops: "\u64cd\u4f5c", edit: "\u7de8\u96c6", del: "\u524a\u9664", confirmDelete: "\u3092\u524a\u9664\u3057\u307e\u3059\u304b\uff1f", deleted: "\u524a\u9664\u3057\u307e\u3057\u305f\u3002",
    cardChangeValidate: "\u6c0f\u540d\u3068\u30e1\u30fc\u30eb\u30a2\u30c9\u30ec\u30b9\u3092\u5165\u529b\u3057\u3066\u304f\u3060\u3055\u3044\u3002", cardChangeEmailInvalid: "\u6709\u52b9\u306a\u30e1\u30fc\u30eb\u30a2\u30c9\u30ec\u30b9\u3092\u5165\u529b\u3057\u3066\u304f\u3060\u3055\u3044\u3002", cardChangeSending: "\u9001\u4fe1\u4e2d\u2026", cardChangeDone: "\u304a\u7533\u3057\u8fbc\u307f\u3092\u53d7\u3051\u4ed8\u3051\u307e\u3057\u305f\u3002\u62c5\u5f53\u8005\u3088\u308aLINE\u306b\u3066\u30ab\u30fc\u30c9\u5909\u66f4URL\u3092\u304a\u9001\u308a\u3057\u307e\u3059\u3002", cardChangeError: "\u9001\u4fe1\u306b\u5931\u6557\u3057\u307e\u3057\u305f\u3002\u3057\u3070\u3089\u304f\u7d4c\u3063\u3066\u304b\u3089\u518d\u5ea6\u304a\u8a66\u3057\u304f\u3060\u3055\u3044\u3002",
    inquiryValidate: "\u304a\u554f\u3044\u5408\u308f\u305b\u5185\u5bb9\u3092\u5165\u529b\u3057\u3066\u304f\u3060\u3055\u3044\u3002", inquirySending: "\u9001\u4fe1\u4e2d\u2026", inquiryDone: "\u9001\u4fe1\u3057\u307e\u3057\u305f\u3002\u904b\u55b6\u304b\u3089\u306e\u8fd4\u4fe1\u3092\u304a\u5f85\u3061\u304f\u3060\u3055\u3044\u3002", inquiryError: "\u9001\u4fe1\u306b\u5931\u6557\u3057\u307e\u3057\u305f\u3002\u3057\u3070\u3089\u304f\u7d4c\u3063\u3066\u304b\u3089\u518d\u5ea6\u304a\u8a66\u3057\u304f\u3060\u3055\u3044\u3002", inquiryNotReady: "\u6e96\u5099\u4e2d\u3067\u3059\u3002\u3057\u3070\u3089\u304f\u304a\u5f85\u3061\u304f\u3060\u3055\u3044\u3002", inquirySendAsSuffix: "\u3055\u3093\uff08\u30ed\u30b0\u30a4\u30f3\u4e2d\uff09\u3068\u3057\u3066\u9001\u4fe1\u3055\u308c\u307e\u3059"
  };
  var state = { page: "home", firebaseReady: false, preview: false, auth: null, db: null, user: null, profile: null, contents: [], users: [], directory: [] };
  function byId(id) { return document.getElementById(id); }
  function firebaseConfig() { var config = window.RESKILLING_FIREBASE_CONFIG || {}; return (!config.apiKey || !config.projectId) ? null : config; }
  function hasFirebaseSdk() { return typeof window.firebase !== "undefined" && window.firebase.auth && window.firebase.firestore; } function previewAllowed() { return location.hostname === "localhost" || location.hostname === "127.0.0.1" || new URLSearchParams(location.search).get("preview") === "1"; }
  function showLogin(message) { var login = byId("loginView"); var app = byId("appView"); var msg = byId("loginMsg"); if (login) login.classList.remove("hidden"); if (app) app.classList.add("hidden"); if (msg && message) msg.textContent = message; }
  function showApp() { var login = byId("loginView"); var app = byId("appView"); if (login) login.classList.add("hidden"); if (app) app.classList.remove("hidden"); }
  function escapeHtml(value) { return String(value == null ? "" : value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;"); }
  function normalizeText(value) { return String(value || "").trim().toLowerCase(); }
  function displayDate(value) { if (!value) return "-"; if (typeof value === "string") return value.slice(0, 10); if (value.toDate) return value.toDate().toISOString().slice(0, 10); return String(value).slice(0, 10); }
  function dateValue(item) { var value = item.publishedAt || item.uploadedAt || ""; if (value && value.toDate) return value.toDate().getTime(); var parsed = Date.parse(value); return Number.isNaN(parsed) ? 0 : parsed; }
  function isAdmin() { return state.profile && state.profile.role === "admin"; }
  function isEditor() { return state.profile && (state.profile.role === "admin" || state.profile.role === "editor"); }
  function isViewerRole() { return state.profile && state.profile.role === "viewer"; }
  function youtubeUrl(item) { var u = String((item && item.url) || ""); return /^https?:\/\//i.test(u) ? u : "https://www.youtube.com/@phama_cam"; }
  function isYoutube(url) { return /^https?:\/\/(www\.)?(youtube\.com|youtu\.be)\//i.test(url || ""); }
  function setChrome() { if (byId("userName")) byId("userName").textContent = (state.profile && state.profile.name) || (state.user && state.user.displayName) || (state.user && state.user.email) || "Preview"; if (byId("roleBadge")) byId("roleBadge").textContent = state.preview ? "preview" : ((state.profile && state.profile.role) || "member"); if (byId("adminLink")) byId("adminLink").classList.toggle("hidden", !(isEditor() || isViewerRole())); if (byId("adminCta")) byId("adminCta").classList.toggle("hidden", !(isEditor() || isViewerRole())); if (byId("snsLink")) byId("snsLink").classList.toggle("hidden", !isEditor()); if (byId("loginBtn") && !state.firebaseReady) byId("loginBtn").textContent = TXT.preview; }
  function toast(message) { var mount = byId("toastMount"); if (!mount) return; var el = document.createElement("div"); el.className = "toast"; el.textContent = message; mount.appendChild(el); window.setTimeout(function () { el.remove(); }, 3200); }
  async function loadSampleContents() { try { var response = await fetch("data/contents.json", { cache: "no-store" }); if (!response.ok) throw new Error("sample load failed"); return await response.json(); } catch (error) { return [{ id: "fallback", title: TXT.videoTitle, category: TXT.basic, type: "video", url: "https://www.youtube.com/@phama_cam", note: TXT.sampleNote, published: true, publishedAt: "2026-06-30", order: 1 }]; } }
  async function enterPreview() { state.preview = true; state.user = { email: "preview@wise-jmco.com", displayName: "Preview" }; state.profile = { email: state.user.email, name: "Preview", role: "member", status: "active" }; state.contents = await loadSampleContents(); setChrome(); showApp(); renderPage(); }
  async function loadProfile(user) { var email = normalizeText(user.email); var ref = state.db.collection("users").doc(email); var snap = await ref.get(); if (!snap.exists) { if (email !== ADMIN_EMAIL) return null; var bootstrap = { email: email, name: user.displayName || "Director", role: "admin", status: "active", createdAt: window.firebase.firestore.FieldValue.serverTimestamp(), updatedAt: window.firebase.firestore.FieldValue.serverTimestamp() }; await ref.set(bootstrap); return bootstrap; } return snap.data(); }
  async function loadContents() { if (!state.firebaseReady) { state.contents = await loadSampleContents(); return; } if (isViewerRole()) { state.contents = []; return; } var snap = await state.db.collection("contents").where("published", "==", true).get(); state.contents = snap.docs.map(function (doc) { return Object.assign({ id: doc.id }, doc.data()); }).sort(function (a, b) { var oa = a.order != null ? Number(a.order) : Infinity; var ob = b.order != null ? Number(b.order) : Infinity; return oa - ob; }); }
  async function loadAdminData() { if (!isEditor() || !state.firebaseReady) { state.users = state.preview ? [state.profile] : []; return; } if (isAdmin()) { var results = await Promise.all([state.db.collection("users").orderBy("email", "asc").get(), state.db.collection("contents").orderBy("order", "asc").get()]); state.users = results[0].docs.map(function (doc) { return Object.assign({ id: doc.id }, doc.data()); }); state.contents = results[1].docs.map(function (doc) { return Object.assign({ id: doc.id }, doc.data()); }); } else { var contentsSnap = await state.db.collection("contents").orderBy("order", "asc").get(); state.users = []; state.contents = contentsSnap.docs.map(function (doc) { return Object.assign({ id: doc.id }, doc.data()); }); } }
  function contentCard(item) { var url = youtubeUrl(item); var badge = isYoutube(url) ? TXT.youtube : (item.type || "video"); return '<a class="card video-card" href="' + escapeHtml(url) + '" target="_blank" rel="noopener noreferrer" aria-label="' + escapeHtml(item.title || TXT.openVideo) + '"><div class="meta"><span class="type">' + escapeHtml(badge) + '</span><span class="badge">' + escapeHtml(item.category || TXT.uncategorized) + '</span></div><h3>' + escapeHtml(item.title) + '</h3><p class="muted">' + escapeHtml(item.note || "") + '</p><div class="meta"><span>' + escapeHtml(displayDate(item.publishedAt || item.uploadedAt)) + '</span></div><span class="btn secondary small card-action">' + TXT.openVideo + '</span></a>'; }
  function sortedContents(items) { var sort = (byId("sortFilter") && byId("sortFilter").value) || "newest"; return items.slice().sort(function (a, b) { if (sort === "oldest") return dateValue(a) - dateValue(b); if (sort === "category") return String(a.category || "").localeCompare(String(b.category || ""), "ja") || (dateValue(b) - dateValue(a)); return dateValue(b) - dateValue(a); }); }
  function renderHome() { var published = sortedContents(state.contents.filter(function (item) { return item.published !== false; })); var categories = new Set(published.map(function (item) { return item.category; }).filter(Boolean)); if (byId("statPublished")) byId("statPublished").textContent = String(published.length); if (byId("statCategories")) byId("statCategories").textContent = String(categories.size); if (byId("statLatest")) byId("statLatest").textContent = displayDate(published[0] && (published[0].publishedAt || published[0].uploadedAt)); if (byId("latestList")) byId("latestList").innerHTML = published.slice(0, 6).map(contentCard).join("") || '<div class="empty">' + TXT.noPublished + '</div>'; var showCard = !state.preview && state.profile && state.profile.status === "active" && state.profile.role !== "viewer"; if (byId("cardChangePanel")) byId("cardChangePanel").classList.toggle("hidden", !showCard); if (showCard) { if (byId("cardChangeName") && !byId("cardChangeName").value) byId("cardChangeName").value = state.profile.name || ""; if (byId("cardChangeEmail") && !byId("cardChangeEmail").value) byId("cardChangeEmail").value = state.profile.email || ""; } var inquiryReady = INQUIRY_GAS_ENDPOINT && INQUIRY_GAS_ENDPOINT.indexOf("PASTE_") !== 0; if (byId("inquiryPanel")) byId("inquiryPanel").classList.toggle("hidden", !showCard || !inquiryReady); if (showCard && byId("inquirySenderInfo")) byId("inquirySenderInfo").textContent = ((state.profile.name || state.profile.email || "") + TXT.inquirySendAsSuffix); }
  function renderVideos() { var list = byId("videoList"); if (!list) return; var searchInput = byId("searchInput"); var categoryFilter = byId("categoryFilter"); var categories = Array.from(new Set(state.contents.map(function (item) { return item.category; }).filter(Boolean))).sort(function (a, b) { return a.localeCompare(b, "ja"); }); if (categoryFilter && categoryFilter.options.length <= 1) categoryFilter.insertAdjacentHTML("beforeend", categories.map(function (category) { return '<option value="' + escapeHtml(category) + '">' + escapeHtml(category) + '</option>'; }).join("")); var keyword = normalizeText(searchInput && searchInput.value); var category = (categoryFilter && categoryFilter.value) || ""; var filtered = state.contents.filter(function (item) { return (!keyword || normalizeText(item.title).includes(keyword)) && (!category || item.category === category); }); filtered = sortedContents(filtered); list.innerHTML = filtered.map(contentCard).join("") || '<div class="empty">' + TXT.noMatch + '</div>'; }
  function renderStaffTable() { var mount = byId("staffTable"); if (!mount) return; var staffUsers = state.users.filter(function (u) { return u.role === "admin" || u.role === "editor"; }); var selfEmail = (state.profile && state.profile.email) || normalizeText((state.user && state.user.email) || ""); var rows = staffUsers.map(function (user) { var dis = user.email === selfEmail ? ' disabled' : ''; return '<tr><td>' + escapeHtml(user.email) + '</td><td>' + escapeHtml(user.name || "") + '</td><td>' + escapeHtml(user.role) + '</td><td>' + escapeHtml(user.status) + '</td><td><button class="btn secondary small" data-act="edit" data-email="' + escapeHtml(user.email) + '"' + dis + '>' + TXT.edit + '</button> <button class="btn secondary small" data-act="del" data-email="' + escapeHtml(user.email) + '"' + dis + '>' + TXT.del + '</button></td></tr>'; }).join(""); mount.innerHTML = '<table><thead><tr><th>' + TXT.mail + '</th><th>' + TXT.name + '</th><th>' + TXT.role + '</th><th>' + TXT.status + '</th><th>' + TXT.ops + '</th></tr></thead><tbody>' + (rows || '<tr><td colspan="5">' + TXT.noStaff + '</td></tr>') + '</tbody></table>'; }
  function renderMemberTable() { var mount = byId("memberTable"); if (!mount) return; var memberUsers = state.users.filter(function (u) { return u.role === "member"; }); var selfEmail = (state.profile && state.profile.email) || normalizeText((state.user && state.user.email) || ""); var rows = memberUsers.map(function (user) { var dis = user.email === selfEmail ? ' disabled' : ''; return '<tr><td>' + escapeHtml(user.email) + '</td><td>' + escapeHtml(user.name || "") + '</td><td>' + escapeHtml(user.role) + '</td><td>' + escapeHtml(user.status) + '</td><td><button class="btn secondary small" data-act="edit" data-email="' + escapeHtml(user.email) + '"' + dis + '>' + TXT.edit + '</button> <button class="btn secondary small" data-act="del" data-email="' + escapeHtml(user.email) + '"' + dis + '>' + TXT.del + '</button></td></tr>'; }).join(""); mount.innerHTML = '<table><thead><tr><th>' + TXT.mail + '</th><th>' + TXT.name + '</th><th>' + TXT.role + '</th><th>' + TXT.status + '</th><th>' + TXT.ops + '</th></tr></thead><tbody>' + (rows || '<tr><td colspan="5">' + TXT.noUsers + '</td></tr>') + '</tbody></table>'; var memberSummary = byId("memberTableSummary"); if (memberSummary) memberSummary.textContent = "\u4e00\u89a7\u3092\u8868\u793a\uff08" + memberUsers.length + "\u4ef6\uff09"; }
function renderContentTable() { var mount = byId("contentTable"); if (!mount) return; var rows = state.contents.map(function (item) { return '<tr><td>' + escapeHtml(item.title) + '</td><td>' + escapeHtml(item.category) + '</td><td>' + escapeHtml(item.type) + '</td><td>' + escapeHtml(displayDate(item.publishedAt || item.uploadedAt)) + '</td><td>' + (item.published === false ? TXT.private : TXT.public) + '</td></tr>'; }).join(""); mount.innerHTML = '<table><thead><tr><th>' + TXT.title + '</th><th>' + TXT.category + '</th><th>' + TXT.type + '</th><th>' + TXT.publishDate + '</th><th>' + TXT.status + '</th></tr></thead><tbody>' + (rows || '<tr><td colspan="5">' + TXT.noVideos + '</td></tr>') + '</tbody></table>'; var contentSummary = byId("contentTableSummary"); if (contentSummary) contentSummary.textContent = "\u4e00\u89a7\u3092\u8868\u793a\uff08" + state.contents.length + "\u4ef6\uff09"; }
  async function saveMember() { if (!isAdmin() || !state.firebaseReady) { toast(TXT.firebaseSave); return; } var email = normalizeText(byId("memberEmail") && byId("memberEmail").value); if (!email) { toast(TXT.enterEmail); return; } var payload = { email: email, name: (byId("memberName") && byId("memberName").value.trim()) || "", role: (byId("memberRole") && byId("memberRole").value) || "member", status: (byId("memberStatus") && byId("memberStatus").value) || "active", updatedAt: window.firebase.firestore.FieldValue.serverTimestamp() }; await state.db.collection("users").doc(email).set(payload, { merge: true }); await loadAdminData(); renderStaffTable(); renderMemberTable(); toast(TXT.savedMember); }
  function editMember(email) { var user = state.users.find(function (u) { return u.email === email; }); if (!user) return; if (byId("memberEmail")) byId("memberEmail").value = user.email; if (byId("memberName")) byId("memberName").value = user.name || ""; if (byId("memberRole")) byId("memberRole").value = user.role || "member"; if (byId("memberStatus")) byId("memberStatus").value = user.status || "active"; var anchor = byId("memberEmail"); if (anchor) anchor.scrollIntoView({ behavior: "smooth" }); }
  async function deleteMember(email) { if (!isAdmin() || !state.firebaseReady) { toast(TXT.adminOnly); return; } if (!window.confirm(email + TXT.confirmDelete)) return; await state.db.collection("users").doc(email).delete(); await loadAdminData(); renderStaffTable(); renderMemberTable(); toast(TXT.deleted); }
  async function loadDirectory() { if (!state.firebaseReady) { state.directory = []; return; } var snap = await state.db.collection("member_directory").get(); state.directory = snap.docs.map(function (doc) { return Object.assign({ id: doc.id }, doc.data()); }).sort(function (a, b) { return String(a.name || "").localeCompare(String(b.name || ""), "ja"); }); }
  function renderDirectoryTable() { var mount = byId("directoryTable"); if (!mount) return; var rows = state.directory.map(function (u) { return '<tr><td>' + escapeHtml(u.name || "") + '</td><td>' + escapeHtml(u.email) + '</td></tr>'; }).join(""); mount.innerHTML = '<table><thead><tr><th>' + TXT.name + '</th><th>' + TXT.mail + '</th></tr></thead><tbody>' + (rows || '<tr><td colspan="2">' + TXT.noUsers + '</td></tr>') + '</tbody></table>'; var count = byId("directoryCount"); if (count) count.textContent = state.directory.length + '\u540d'; var dirSummary = byId("directoryTableSummary"); if (dirSummary) dirSummary.textContent = "\u4e00\u89a7\u3092\u8868\u793a\uff08" + state.directory.length + "\u4ef6\uff09"; }
  function downloadDirectoryCsv() { var header = '\u6c0f\u540d,\u30e1\u30fc\u30eb\u30a2\u30c9\u30ec\u30b9'; var lines = state.directory.map(function (u) { return '"' + String(u.name || "").replace(/"/g, '""') + '","' + String(u.email).replace(/"/g, '""') + '"'; }); var csv = '\ufeff' + header + '\r\n' + lines.join('\r\n'); var blob = new Blob([csv], { type: 'text/csv;charset=utf-8' }); var d = new Date(); var stamp = String(d.getFullYear()) + ('0' + (d.getMonth() + 1)).slice(-2) + ('0' + d.getDate()).slice(-2); var a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = 'salon_members_' + stamp + '.csv'; document.body.appendChild(a); a.click(); a.remove(); URL.revokeObjectURL(a.href); }
  async function submitCardChangeRequest() { if (!state.firebaseReady) { toast("Firebase\u672a\u8a2d\u5b9a\u306e\u305f\u3081\u9001\u4fe1\u3067\u304d\u307e\u305b\u3093\u3002"); return; } var nameVal = (byId("cardChangeName") && byId("cardChangeName").value.trim()) || ""; var emailVal = (byId("cardChangeEmail") && byId("cardChangeEmail").value.trim()) || ""; var noteVal = (byId("cardChangeNote") && byId("cardChangeNote").value.trim()) || ""; var msgEl = byId("cardChangeMsg"); var btn = byId("cardChangeBtn"); if (msgEl) { msgEl.textContent = ""; msgEl.style.color = ""; } if (!nameVal || !emailVal) { if (msgEl) { msgEl.textContent = TXT.cardChangeValidate; msgEl.style.color = "var(--danger)"; } return; } if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(emailVal)) { if (msgEl) { msgEl.textContent = TXT.cardChangeEmailInvalid; msgEl.style.color = "var(--danger)"; } return; } if (btn) { btn.disabled = true; btn.textContent = TXT.cardChangeSending; } try { await state.db.collection("card_change_requests").add({ name: nameVal, email: emailVal, note: noteVal, status: "pending", source: "member-site", createdAt: window.firebase.firestore.FieldValue.serverTimestamp() }); if (msgEl) { msgEl.textContent = TXT.cardChangeDone; msgEl.style.color = "var(--success)"; } if (byId("cardChangeName")) byId("cardChangeName").value = ""; if (byId("cardChangeEmail")) byId("cardChangeEmail").value = ""; if (byId("cardChangeNote")) byId("cardChangeNote").value = ""; } catch (err) { if (msgEl) { msgEl.textContent = TXT.cardChangeError; msgEl.style.color = "var(--danger)"; } } finally { if (btn) { btn.disabled = false; btn.textContent = "\u7533\u3057\u8fbc\u3080"; } } }
  async function submitInquiry() { var msgEl = byId("inquiryMsg"); var btn = byId("inquiryBtn"); if (msgEl) { msgEl.textContent = ""; msgEl.style.color = ""; } if (!INQUIRY_GAS_ENDPOINT || INQUIRY_GAS_ENDPOINT.indexOf("PASTE_") === 0) { if (msgEl) { msgEl.textContent = TXT.inquiryNotReady; msgEl.style.color = "var(--danger)"; } return; } var messageVal = (byId("inquiryMessage") && byId("inquiryMessage").value.trim()) || ""; if (!messageVal) { if (msgEl) { msgEl.textContent = TXT.inquiryValidate; msgEl.style.color = "var(--danger)"; } return; } var nameVal = (state.profile && state.profile.name) || ""; var emailVal = (state.profile && state.profile.email) || ""; if (btn) { btn.disabled = true; btn.textContent = TXT.inquirySending; } try { var response = await fetch(INQUIRY_GAS_ENDPOINT, { method: "POST", headers: { "Content-Type": "text/plain" }, body: JSON.stringify({ name: nameVal, email: emailVal, message: messageVal }) }); var result = await response.json().catch(function () { return null; }); if (!response.ok || !result || result.ok !== true) throw new Error((result && result.error) || "inquiry request failed"); if (msgEl) { msgEl.textContent = TXT.inquiryDone; msgEl.style.color = "var(--success)"; } if (byId("inquiryMessage")) byId("inquiryMessage").value = ""; } catch (err) { if (msgEl) { msgEl.textContent = TXT.inquiryError; msgEl.style.color = "var(--danger)"; } } finally { if (btn) { btn.disabled = false; btn.textContent = "\u9001\u4fe1\u3059\u308b"; } } }
  async function renderAdmin() { if (!isEditor() && !isViewerRole()) { var content = document.querySelector(".content"); if (content) content.insertAdjacentHTML("afterbegin", '<section class="panel"><p class="error">' + TXT.adminOrEditorOnly + '</p></section>'); return; } var syncPanel = byId("syncPanel"); var staffPanel = byId("staffPanel"); var memberPanel = byId("memberPanel"); var directoryPanel = byId("directoryPanel"); var contentDataPanel = byId("contentDataPanel"); if (isViewerRole()) { if (syncPanel) syncPanel.classList.add("hidden"); if (staffPanel) staffPanel.classList.add("hidden"); if (memberPanel) memberPanel.classList.add("hidden"); if (contentDataPanel) contentDataPanel.classList.add("hidden"); if (directoryPanel) directoryPanel.classList.remove("hidden"); await loadDirectory(); renderDirectoryTable(); return; } await loadAdminData(); if (!isAdmin()) { if (syncPanel) syncPanel.classList.add("hidden"); if (staffPanel) staffPanel.classList.add("hidden"); if (memberPanel) memberPanel.classList.add("hidden"); if (directoryPanel) directoryPanel.classList.add("hidden"); } else { if (syncPanel) syncPanel.classList.remove("hidden"); if (staffPanel) staffPanel.classList.remove("hidden"); if (memberPanel) memberPanel.classList.remove("hidden"); if (directoryPanel) directoryPanel.classList.remove("hidden"); renderStaffTable(); renderMemberTable(); await loadDirectory(); renderDirectoryTable(); } renderContentTable(); }
function renderPage() { if (state.page === "home") renderHome(); if (state.page === "videos") renderVideos(); if (state.page === "admin") renderAdmin(); }
  function bindTableEvents(mountId) { var mount = byId(mountId); if (!mount) return; mount.addEventListener("click", function (e) { var btn = e.target.closest('button[data-act]'); if (!btn || btn.disabled) return; var act = btn.getAttribute('data-act'); var email = btn.getAttribute('data-email'); if (act === 'edit') { editMember(email); } if (act === 'del') { deleteMember(email).catch(function (err) { toast(err.message); }); } }); }
  function bindCommonEvents() { if (byId("loginBtn")) byId("loginBtn").addEventListener("click", async function () { if (!state.firebaseReady && previewAllowed()) { await enterPreview(); toast(TXT.previewToast); return; } await state.auth.signInWithPopup(new window.firebase.auth.GoogleAuthProvider()); }); if (byId("logoutBtn")) byId("logoutBtn").addEventListener("click", async function () { if (state.firebaseReady) await state.auth.signOut(); state.preview = false; showLogin(); }); if (byId("searchInput")) byId("searchInput").addEventListener("input", renderVideos); if (byId("categoryFilter")) byId("categoryFilter").addEventListener("change", renderVideos); if (byId("sortFilter")) byId("sortFilter").addEventListener("change", renderVideos); if (byId("saveMemberBtn")) byId("saveMemberBtn").addEventListener("click", function () { saveMember().catch(function (error) { toast(error.message); }); }); if (byId("csvDlBtn")) byId("csvDlBtn").addEventListener("click", downloadDirectoryCsv); if (byId("cardChangeBtn")) byId("cardChangeBtn").addEventListener("click", function() { submitCardChangeRequest().catch(function(err) { toast(err.message); }); }); if (byId("inquiryBtn")) byId("inquiryBtn").addEventListener("click", function() { submitInquiry().catch(function(err) { toast(err.message); }); }); bindTableEvents("staffTable"); bindTableEvents("memberTable"); }
  async function initFirebase() { var config = firebaseConfig(); if (!config || !hasFirebaseSdk()) return false; window.firebase.initializeApp(config); state.auth = window.firebase.auth(); state.db = window.firebase.firestore(); return true; }
  async function init(page) { state.page = page; bindCommonEvents(); state.firebaseReady = await initFirebase(); if (!state.firebaseReady) { if (byId("loginMsg")) byId("loginMsg").textContent = previewAllowed() ? TXT.previewMsg : "会員ログインの準備中です。"; if (byId("loginBtn")) { byId("loginBtn").textContent = previewAllowed() ? TXT.preview : "準備中"; byId("loginBtn").disabled = !previewAllowed(); } if (previewAllowed()) await enterPreview(); else showLogin(); return; } state.auth.onAuthStateChanged(async function (user) { if (!user) { showLogin(); return; } try { var profile = await loadProfile(user); if (!profile || profile.status !== "active") { await state.auth.signOut(); showLogin(TXT.noMember); return; } state.user = user; state.profile = profile; await loadContents(); setChrome(); showApp(); renderPage(); } catch (error) { showLogin(error.message); } }); }
  window.ReskillApp = { init: init };
})();
